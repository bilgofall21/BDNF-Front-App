import { TextEditorPipe } from './text-editor.pipe';

describe('TextEditorPipe', () => {
  it('create an instance', () => {
    const pipe = new TextEditorPipe();
    expect(pipe).toBeTruthy();
  });
});
